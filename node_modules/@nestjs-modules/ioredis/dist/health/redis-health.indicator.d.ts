import { HealthIndicatorResult, HealthIndicatorService } from '@nestjs/terminus';
import Redis, { Cluster } from 'ioredis';
export declare class RedisHealthIndicator {
    private readonly redis;
    private readonly healthIndicatorService;
    constructor(redis: Redis | Cluster | null, healthIndicatorService: HealthIndicatorService);
    isHealthy(key: string, connection?: Redis | Cluster): Promise<HealthIndicatorResult>;
}
